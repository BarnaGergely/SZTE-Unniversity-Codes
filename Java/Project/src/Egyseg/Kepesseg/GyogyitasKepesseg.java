package Egyseg.Kepesseg;

import Public.*;
import Egyseg.*;


public class GyogyitasKepesseg extends Kepesseg{
    public GyogyitasKepesseg(String name) {
        super(name);
    }

    /*
    public Csateter.EgysegCsataban[][] hasznal(Csateter.EgysegCsataban matrix[][], Koordinata hely, int sebzes){
        Csateter.EgysegCsataban ujMatrix[][] = new Csateter.EgysegCsataban[10][12];
        ujMatrix = matrix;
        for (int i = 0; i < 10; i++) {
            for (int j = 0; j < 12; j++) {
                if (hely.getSzelesseg() == j && hely.getHosszusag() == i) {
                   ujMatrix.egyseg.
                }
            }
        }
    return matrix;
    }*/
}
